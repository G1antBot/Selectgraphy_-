# 片刻 (Pianke) 待更新与优化列表

这份文档记录了本项目未来需要优先开发和优化的核心功能。**如果您是未来的 AI 编程助手，请阅读以下具体的需求背景和实现思路，协助开发者完成这些优化。**

---

## 优先级 1：硬件感知（GPU 加速状态可视化与提示）

**需求背景：**
目前程序在“专家模式”下使用了 `torch` 和 `onnxruntime` 等重型 AI 框架进行特征提取。但在 Windows 系统下，如果用户未正确配置 CUDA，模型会自动降级到 CPU 运行，导致速度极慢且用户不知情。需要让用户对当前的硬件算力状态有直观的感知。

**AI 实施指南（开发思路）：**
1. **后端扩展 (`app.py`)**：
   - 在启动或提供能力的 API（如 `/api/capabilities`）中，增加探针代码。
   - 检测 PyTorch 状态：`torch.cuda.is_available()` 或对 Mac 的 `torch.backends.mps.is_available()`。
   - 检测 ONNXRuntime 状态：检查 `onnxruntime.get_available_providers()` 是否包含 `CUDAExecutionProvider` / `CoreMLExecutionProvider`。
   - 返回当前设备的算力等级（如 `GPU (CUDA)`, `GPU (MPS)`, `CPU`）。
2. **前端交互 (`index.html` / `app.js` 等)**：
   - 在前端首页或进度条旁边新增一个小组件（Badge），显示当前的硬件状态。
   - 如果检测到是 `CPU` 模式且在 Windows 系统下，提供一个 ⚠️ 警告图标，悬停或点击时弹出提示：“当前以 CPU 慢速模式运行，如需开启显卡加速请配置 CUDA 环境”。
   - 在扫描过程中，计算并实时显示处理速度（如 `x.x 张/秒`）。

---

## 优先级 2：项目管理（多相册历史与近期任务列表）

**需求背景：**
目前软件采用“单次单文件夹”会话设计，用户每次打开都要手动复制粘贴照片路径。为了适应摄影师一天产生多个项目文件夹（内景、外景等）、分批次选片的工作流，需要增加历史记录功能，允许一键恢复之前的选片进度。

**AI 实施指南（开发思路）：**
1. **全局状态存储 (`app.py`)**：
   - 现有的进度保存在各相册的 `.pic_selecter_state.json` 中，但缺乏全局索引。
   - 需要在操作系统的用户级配置目录下（如 `~/.config/pic_selecter/history.json`，可参考当前保存 `ARK_API_KEY` 的逻辑），维护一个最近打开项目列表（List of dicts）。
   - 包含字段：`folder_path` (路径), `last_opened` (最后打开时间), `progress` (如“已选 120/300”的文本摘要或状态)。
2. **后端 API (`app.py`)**：
   - 新增 `GET /api/history`：返回按时间倒序排列的近期相册列表。
   - 拦截或扩展启动分析（`/api/start`）的逻辑：当成功读取到一个相册时，将其写入/更新到全局 `history.json` 中。
3. **前端首页 (`index.html` / `style.css`)**：
   - 在路径输入框下方，增加一个“最近项目 (Recent Projects)”区块。
   - 用卡片或列表的形式展示最近的 3~5 个相册。
   - 点击某一项时，自动将该路径填入输入框，并触发加载。

---

## 优先级 3：包围曝光 (HDR) 与全景接片的智能识别与豁免保护

**需求背景：**
风光摄影师大量使用包围曝光（连拍过曝、正常、欠曝三张）或摇摄拍全景接片。目前的 AI 很容易把那张欠曝/过曝的当成“废片”自动淘汰。需要识别这些特殊序列并给予“免死金牌”。

**AI 实施指南（开发思路）：**
1. **EXIF 数据解析 (`grouper.py` 或 `app.py`)**：
   - 扩展当前的元数据提取逻辑，重点提取照片的曝光补偿值 (Exposure Bias) 或快门速度，以及精确的连拍时间戳。
2. **特殊序列识别算法**：
   - 如果发现同组（时间间隔极短且基础构图近似）内存在规律的 EV 步进变化（如 -2, 0, +2），将其标记为 `is_hdr_bracket = True`。
   - 如果发现同组内照片时间间隔极短，但 DINOv2 特征发生连续规律的平移偏移（无突然断层），识别为 `is_panorama_sequence = True`。
3. **初筛豁免逻辑 (`app.py`)**：
   - 在判断废片的函数 `_fatal_flags` 或 `_auto_reject_reason` 中：当遇到这两种特殊序列标签时，立刻跳过 `underexposed` (欠曝) 和 `overexposed` (过曝) 的致命错误判定。
   - 在前端 UI 的列表中，将这几张照片打包显示为一个独立区块，并明确加上 `[HDR 序列]` 或 `[全景素材]` 的标签徽章，提醒摄影师不要拆散它们。
