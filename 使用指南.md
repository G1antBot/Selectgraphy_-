# 片刻 (Pianke) 使用指南

**片刻** 是一款专为摄影师和摄影爱好者设计的本地照片初步筛选与分组工具。通过将相似照片自动分组并提供“左右擂台 PK”的交互方式，帮助您快速从大量连拍或相似照片中挑选出最满意的作品。

本指南将带您了解如何高效使用“片刻”进行选片。

---

## 1. 启动与准备

### 1.1 启动软件
- **Windows 用户**：双击项目文件夹内的 `启动_Windows.bat`，若弹出安全提示，请点击“更多信息” ➔ “仍要运行”。
- **macOS 用户**：双击 `启动_macOS.command`，若提示身份不明，请按住 Control 键点击脚本，选择“打开”。

启动后，系统会自动在您的默认浏览器中打开操作界面（默认地址为 `http://localhost:5057`）。

### 1.2 选择工作模式
在首页，您需要根据电脑配置和拍摄内容选择合适的模式：
- **极速 (Fast) 模式**：完全本地运行，速度极快，适合电脑配置较低、或主要拍摄风景/静物/产品图的用户。
- **专家 (Expert) 模式**：内置本地 AI 模型（首次运行需下载约 600MB 模型），识别更为精准，特别是针对人像、婚礼、活动等，能准确识别“同人不同姿势不同背景”。
- **土豪 (Tycoon) 模式**：需配置第三方大模型 API Key（如豆包、GPT-4V 等），大模型会用自然语言解释为什么淘汰这张照片（比如“左侧闭眼了”）。注意：此模式需要联网。

---

## 2. 选片工作流

### 步骤一：导入照片
1. 选择好工作模式后。
2. 在输入框中粘贴您需要筛选的**照片文件夹绝对路径**，或者直接将文件夹拖拽到指定区域。
3. 如果您有特殊需求（如希望复制而非移动照片以保留原文件位置），可以在“更多选项”中将归档模式修改为**复制模式**。
4. 点击 **开始整理**。

### 步骤二：AI 分析与初筛
- 导入后，系统会流式显示扫描进度和剩余时间，耐心等待其完成分析。
- 系统会自动剔除存在明显技术问题的照片（如严重失焦模糊、过曝、欠曝、闭眼等）。

### 步骤三：初筛复核（拯救被淘汰的照片）
- 分析完成后，会平铺展示所有被判定为“废片”的照片及淘汰原因。
- **拯救操作**：如果您认为某张照片被误判，直接**点击该照片**，即可将其放回待分组列表中。
- 未点击的照片将被直接归入最终的淘汰区。

### 步骤四：分组预览与调整
- 系统会将视觉上相似、拍摄时间相近的“同一个瞬间”的照片归为一组。
- **实时调参**：如果您觉得分组过细（同组照片被分开了）或过粗（不同场景分在了一起），可以通过拖动界面上的滑块调节阈值，并点击 **重新分组** 即时查看效果。

### 步骤五：擂台 PK（核心操作）
进入相似组内的两两对决界面。请将双手放在键盘上，使用以下快捷键进行极速筛选：

| 快捷键 | 动作 | 说明 |
| :--- | :--- | :--- |
| `←` (左方向键) | **保留左侧** | 淘汰右侧照片 |
| `→` (右方向键) | **保留右侧** | 淘汰左侧照片 |
| `↑` (上方向键) | **全要** | 两张都保留 |
| `↓` (下方向键) | **全不要** | 两张都淘汰 |
| `[` | **仅淘汰左侧** | （通常用于处理左侧单张图像损坏的情况） |
| `]` | **仅淘汰右侧** | （通常用于处理右侧单张图像损坏的情况） |
| `S` | **跳过本组** | 暂不处理，将该组移至队尾最后再看 |
| `Z` | **切换缩放** | 在 1x、2x、4x 间循环放大，左右两图会**同步平移**方便对比细节 |
| `Shift` + `Z` | **撤销** | 撤销当前组的上一步操作 |

> **提示**：软件支持多级撤销（单步撤销、整组重选、全局重做）以及进度实时保存。若中途关闭浏览器，下次重新导入该文件夹即可恢复进度。

### 步骤六：完成与水印
- 筛选完成后，将进入 Winner 总览页，展示所有胜出的保留照片。
- **一键水印**：点击 **相机水印**，系统可自动为您胜出的照片批量添加包含机型、镜头、参数（快门/光圈/ISO）及品牌 Logo 的精美边框水印。系统内置了多达 11 种水印风格（详尽、极简、拍立得风等）供您选择。

---

## 3. 输出结果在哪里？

处理完成后，您的原文件夹结构会发生如下变化：

```text
📁 您的原照片文件夹/
├── 📂 winners/                # 最终胜出保留的照片 (成片)
├── 📂 losers/                 # 被淘汰的照片 (废片)
├── 📄 .pic_selecter_state.json # 进度存档文件 (请勿手动修改)
└── 📂 _pic_selecter/          # 系统缓存目录 (含缩略图和日志)
```
- **移动模式（默认）** 下，您的原图会被物理移动到 `winners/` 或 `losers/`。不用担心，由于支持撤销，软件可以无损将文件搬回原位。
- 水印照片将输出在 `winners/watermarked_<时间戳>/` 目录下。

---

## 4. 常见问题解答

**Q: RAW 格式支持吗？**
支持。如果是 RAW+JPG 双格式拍摄，系统会分析 JPG 以保证极速，在操作时会将对应的 RAW 甚至 XMP 文件打包同步移动。如果是纯 RAW，系统也能毫秒级提取内嵌预览图进行比对。

**Q: 中途退出会丢失进度吗？**
不会。进度会实时保存在照片目录的 `.pic_selecter_state.json` 中。再次启动软件处理同一个文件夹时，会自动接着上次的进度继续。

**Q: 想要清理空间，如何删除缓存？**
如果您已经选片完毕并导出了最终照片，可以直接删除照片目录下的 `_pic_selecter/` 文件夹和 `.pic_selecter_state.json` 文件以释放空间。
